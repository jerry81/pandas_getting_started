# lesson 4 - read csv

- big data sets may be stored with csv
- use to_string() to print entire data frame, otherwise pandas only returns first and last 5 rows
- customize the above with pd.options.display.max_rows

- TIL: relative path is resolved from CWD, so running from parent directory resulted in not finding the file